resources:
    ${instance}-member:
        type: OS::Neutron::PoolMember
        properties:
          pool_id:
            get_resource: ${name}-pool
          address:
            get_attr:
              - $instance
              - first_address
          protocol_port: $port
